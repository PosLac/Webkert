{
    Now when you will make modifications to any files or folders, follow the same procedure in step 4 above to commit your changes.
    git status
    git add .
    git commit -m "changes in the index.html"
    git push
    VAGY
    VS Code-ban
}
{
    SCSS: lehetnek benne változók ($bgcolor) és osztályok
    lehet h váltani kell majd SCSS-re
}
{
    modulokban vannak a componentek, akár több is (app.module.ts/declarations:)
    ng generate component pages/main --module=app
    
    @Component({
    selector: 'app-main',
    template: '<h1>Fejléc</h1>',
    styleUrls: ['./main.component.css']
    })
    ilyet is lehet
}
{
    #chosenimage
    html változó

    (change)="reload()"
    változást figyelő esemény html kódban
}
{
    <ng-content></ng-content>
    az app.component.html-ben az adott component tagjei közé írt 
    cuccot jeleníti meg
}
{
    struktúrális direktívák: if/switch/for, *-gal jelölik, ()=output, []=input
    egyirányú adatösszekötés: {{változó}} html-ben
    kétirányú: 
    ngSwitchDefault
    8

    Don't use [(ngModel)]="password" and #password both together. I got same error and just fixed removing #password.
    
      //spread operator: uj objektumot hoz letre
      this.comments.push(...this.commentObject);
      ugyanaz:
      this.comments.push(Object.assign({}this.commentObject));
}
{
    modulokba szervezés:main/gallery/contacts
    ng generate module pages/not-found --route=not-found --module=app.module
    observable: nyitott adatfolyam
}